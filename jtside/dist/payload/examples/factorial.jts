# Comprehensive program - Factorial calculator

func factorial(n)
    if n <= 1
        return 1
    else
        return n * factorial(n - 1)
    end
end

say("Factorials:")
for i of 0 to 11
    say(i + "! = " + factorial(i))
end
