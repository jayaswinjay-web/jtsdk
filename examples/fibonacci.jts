# Fibonacci sequence

func fibonacci(n)
    if n <= 1
        return n
    end
    return fibonacci(n - 1) + fibonacci(n - 2)
end

print("Fibonacci sequence:")
for i in 0 to 12
    print("fib(" + i + ") = " + fibonacci(i))
end