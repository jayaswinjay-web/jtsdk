class Animal
    func init(self, name)
        self.name = name
    end

    func speak(self)
        say(self.name + " makes a sound")
    end
end

class Dog extends Animal
    func bark(self)
        say(self.name + " barks!")
    end
end

d = new Dog("Rex")
d.speak()
d.bark()
say(d.name)


