# math.stats scroll - statistics functions
func mean(numbers)
    total = 0
    for n of numbers
        total = total + n
    end
    return total / len(numbers)
end

func sum(numbers)
    total = 0
    for n of numbers
        total = total + n
    end
    return total
end
