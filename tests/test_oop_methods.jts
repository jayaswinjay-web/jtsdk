class Animal
    func init(self, name)
        self.name = name
    end

    func speak(self)
        say(self.name + " says hello!")
    end

    func greet(self, other)
        say(self.name + " greets " + other + "!")
    end
end

a = new Animal("Cat")
a.speak()
a.greet("Dog")
say(a.name)


