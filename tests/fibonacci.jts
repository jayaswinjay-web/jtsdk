# Fibonacci sequence

func fibonacci(n)
    if n <= 1
        return n
    end
    return fibonacci(n - 1) + fibonacci(n - 2)
end

say("Fibonacci sequence:")
for i of 0 to 12
    say("fib(" + i + ") = " + fibonacci(i))
end


